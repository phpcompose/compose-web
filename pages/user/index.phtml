<?php
$this->layout = 'layout::main';
/** @var \Compose\Web\Auth\Identity $identity */
?>

<section class="row">
    <div class="col-lg-8">
        <h1 class="h4 mb-3">Welcome back</h1>
        <div class="card shadow-sm">
            <div class="card-body">
                <dl class="row mb-0">
                    <dt class="col-sm-3">Email</dt>
                    <dd class="col-sm-9"><?= htmlspecialchars($identity->getEmail(), ENT_QUOTES, 'UTF-8') ?></dd>
                    <dt class="col-sm-3">Username</dt>
                    <dd class="col-sm-9"><?= htmlspecialchars($identity->getUsername() ?? '-', ENT_QUOTES, 'UTF-8') ?></dd>
                    <dt class="col-sm-3">Roles</dt>
                    <dd class="col-sm-9"><?= htmlspecialchars(implode(', ', $identity->getRoles()) ?: '-', ENT_QUOTES, 'UTF-8') ?></dd>
                </dl>
                <div class="mt-3">
                    <a class="btn btn-primary" href="/user/settings">Update account</a>
                    <a class="btn btn-outline-secondary" href="/auth/logout">Logout</a>
                </div>
            </div>
        </div>
    </div>
</section>

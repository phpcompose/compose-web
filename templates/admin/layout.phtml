<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title><?= htmlspecialchars($this->title ?? 'Admin', ENT_QUOTES, 'UTF-8') ?></title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css">
</head>
<body class="bg-light">
<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="/admin">Admin</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#adminNav" aria-controls="adminNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="adminNav">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item"><a class="nav-link" href="/admin">Dashboard</a></li>
                <?php foreach (($this->helpers->admin->modules() ?? []) as $module): ?>
                    <?php $path = $module['base_path'] ?? '#'; ?>
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="<?= htmlspecialchars($path, ENT_QUOTES, 'UTF-8') ?>" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                            <?php if (!empty($module['icon'])): ?>
                                <i class="<?= htmlspecialchars($module['icon'], ENT_QUOTES, 'UTF-8') ?>"></i>
                            <?php endif; ?>
                            <?= htmlspecialchars($module['display_name'] ?? 'Module', ENT_QUOTES, 'UTF-8') ?>
                        </a>
                        <?php if (!empty($module['actions'])): ?>
                            <ul class="dropdown-menu">
                                <?php foreach ($module['actions'] as $action): ?>
                                    <li>
                                        <a class="dropdown-item" href="<?= htmlspecialchars($action['path'] ?? '#', ENT_QUOTES, 'UTF-8') ?>">
                                            <?php if (!empty($action['icon'])): ?>
                                                <i class="<?= htmlspecialchars($action['icon'], ENT_QUOTES, 'UTF-8') ?>"></i>
                                            <?php endif; ?>
                                            <?= htmlspecialchars($action['name'] ?? 'Action', ENT_QUOTES, 'UTF-8') ?>
                                        </a>
                                    </li>
                                <?php endforeach; ?>
                            </ul>
                        <?php endif; ?>
                    </li>
                <?php endforeach; ?>
            </ul>
            <ul class="navbar-nav ms-auto">
                <li class="nav-item"><a class="nav-link" href="/auth/logout">Logout</a></li>
            </ul>
        </div>
    </div>
</nav>

<div class="container-fluid mt-3">
    <div class="row">
<aside class="col-md-3 col-lg-2 mb-3">
    <div class="list-group shadow-sm">
        <a href="/admin" class="list-group-item list-group-item-action">Home</a>
        <?php foreach (($this->helpers->admin->modules() ?? []) as $module): ?>
            <?php $path = $module['base_path'] ?? '#'; ?>
            <div class="list-group-item">
                <div class="d-flex justify-content-between align-items-center">
                    <div>
                        <?php if (!empty($module['icon'])): ?>
                            <i class="<?= htmlspecialchars($module['icon'], ENT_QUOTES, 'UTF-8') ?>"></i>
                        <?php endif; ?>
                        <a href="<?= htmlspecialchars($path, ENT_QUOTES, 'UTF-8') ?>" class="text-decoration-none">
                            <?= htmlspecialchars($module['display_name'] ?? 'Module', ENT_QUOTES, 'UTF-8') ?>
                        </a>
                    </div>
                </div>
                <?php if (!empty($module['actions'])): ?>
                    <div class="mt-2 ms-3 small">
                        <?php foreach ($module['actions'] as $action): ?>
                            <div>
                                <a href="<?= htmlspecialchars($action['path'] ?? '#', ENT_QUOTES, 'UTF-8') ?>" class="text-decoration-none">
                                    <?php if (!empty($action['icon'])): ?>
                                        <i class="<?= htmlspecialchars($action['icon'], ENT_QUOTES, 'UTF-8') ?>"></i>
                                    <?php endif; ?>
                                    <?= htmlspecialchars($action['name'] ?? 'Action', ENT_QUOTES, 'UTF-8') ?>
                                </a>
                            </div>
                        <?php endforeach; ?>
                    </div>
                <?php endif; ?>
            </div>
        <?php endforeach; ?>
    </div>
</aside>
        <main class="col-md-9 col-lg-10">
            <?= $this->block('content') ?>
        </main>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
